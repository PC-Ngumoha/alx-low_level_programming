#include <stdio.h>

/**
 * main - main entry point of the program
 *
 * Description: prints the name of the current file to the screen
 * followed by a newline
 * Return: Always 0 (Successful)
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
