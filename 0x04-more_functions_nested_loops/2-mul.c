#include "main.h"

/**
 * mul - returns the product of two integers
 * @a: First integer
 * @b: Second integer
 *
 * Description: Takes two integers as arguments, calculates
 * and returns the product of these two numbers.
 * Return: @a * @b
 */
int mul(int a, int b)
{
	return (a * b);
}
